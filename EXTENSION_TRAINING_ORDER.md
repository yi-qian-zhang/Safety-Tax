# 扩展实验：训练顺序对 Safety Tax 的影响

## 🎯 研究问题

**核心问题**：训练顺序是否影响最终模型的安全性-推理能力权衡？

## 📊 实验设计

### 对比组

| 实验组 | 阶段1 | 阶段2 | 代号 |
|--------|-------|-------|------|
| **原设计** | 推理训练 | 安全对齐 | R→S |
| **反向** | 安全对齐 | 推理训练 | S→R |
| **联合** | 推理+安全同时训练 | - | R+S |

## 🤔 理论预期

### Order 1: 推理 → 安全 (R→S，原论文)
```
基座 → [推理强化] → LRM → [安全对齐] → 最终模型
性能: 低推理 → 高推理 → 中推理（因为安全对齐导致下降）
安全: 低安全 → 低安全 → 高安全
```
**特点**：存在 Safety Tax（已证实）

### Order 2: 安全 → 推理 (S→R)
```
基座 → [安全对齐] → 安全模型 → [推理训练] → 最终模型
性能: 低推理 → 低推理 → 高推理？
安全: 低安全 → 高安全 → 低安全？（推理训练可能破坏安全性）
```
**关键问题**：
- 推理训练会不会"遗忘"安全对齐？
- 这涉及到 **catastrophic forgetting** 问题

### Order 3: 联合训练 (R+S)
```
基座 → [同时优化推理和安全] → 最终模型
性能: 低推理 → 中等推理
安全: 低安全 → 中等安全
```
**特点**：可能达到平衡，但两者都不是最优

## 🔬 实验预测

### 预测 1: S→R 会遗忘安全性
```
原因：推理训练使用的数据可能包含不安全内容
结果：安全性从 10% 退化到 40%
结论：这个顺序不推荐
```

### 预测 2: R→S 保留部分推理能力
```
原因：安全对齐只修改部分参数，不会完全覆盖推理能力
结果：推理能力从 60% 降到 45%
结论：这个顺序更稳定（原论文设计）
```

### 预测 3: R+S 达到折中
```
原因：两个目标同时优化，互相制约
结果：推理 50%，安全 20%（都是中等）
结论：可能是最佳平衡点
```

## 📝 实验脚本

### 实验1：安全 → 推理 (反向)
```bash
# 第一阶段：安全对齐
sbatch sft.sh Qwen/Qwen2.5-32B-Instruct 5

# 第二阶段：推理训练（以安全对齐后的模型为基础）
sbatch reasoning_after_safety.sh ckpts/Qwen2.5-32B-Instruct_s1_sft_sft_5
```

### 实验2：联合训练
```bash
# 使用混合数据集（安全+推理）
sbatch joint_training.sh Qwen/Qwen2.5-32B-Instruct
```

## 📊 评估指标

对每个实验组，测量：
1. **最终推理能力** (AIME, Math, GPQA)
2. **最终安全性** (Harmful Score)
3. **训练过程中的遗忘程度**

### 遗忘度量
```python
# 对于 S→R 顺序
safety_before_reasoning = 安全对齐后的安全性
safety_after_reasoning = 推理训练后的安全性
forgetting = safety_before_reasoning - safety_after_reasoning
```

## 🎯 预期贡献

如果实验成功，可以：
1. **证明训练顺序的重要性**
2. **发现是否存在更优的训练策略**
3. **为工业界提供训练流程建议**

## ⚠️ 注意事项

这个扩展实验**建议在完成主实验（GRPO vs SFT）后再做**，因为：
1. 需要更多计算资源
2. 研究范围扩大，可能分散注意力
3. 主实验的发现更直接、更有价值

但如果有充足资源，这是一个很好的扩展方向！
